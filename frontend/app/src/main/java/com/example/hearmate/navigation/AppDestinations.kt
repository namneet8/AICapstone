package com.example.hearmate.navigation // Ensure this package matches

/**
 * Defines the routes for navigation within the app.
 */
object AppDestinations {
    const val LISTENING_ROUTE = "listening"
    const val SETTINGS_ROUTE = "settings"
    // Add other routes here as your app grows
}